# 시드 생성 전용 프로파일.
spring:
  # 웹 서버를 띄우지 않아 생성이 끝나면 프로세스가 알아서 종료된다.
  main:
    web-application-type: none

  # 시드 생성은 캐시 테이블을 읽지도 쓰지도 않는다(generateFresh 사용).
  # 그런데 운영 DB를 열면 파일이 잠겨서 서버가 떠 있는 동안 시드를 못 만든다.
  # 일회용 인메모리 DB로 붙어 그 충돌을 없앤다.
  datasource:
    url: jdbc:h2:mem:seedgen;DB_CLOSE_DELAY=-1
